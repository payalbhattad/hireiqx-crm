# Frontend (bundled into the browser — safe: anon key is protected by RLS)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-side only (used by api/ serverless functions — NEVER add a VITE_ prefix)
SUPABASE_SERVICE_ROLE_KEY=
RESEND_API_KEY=
# Optional: verified Resend sender, e.g. "HireIQX CRM <sales@hireiqx.com>"
EMAIL_FROM=
